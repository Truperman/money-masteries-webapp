# Money Masteries — To Do (not started, for later)

## Ask Genie chat widget
- [ ] Keep the glass chat widget present across ALL pages members navigate to
      (not just wherever it currently mounts) — persistent site-wide, not
      per-page.
- [ ] Add a minimize button that collapses the widget down to the small
      round bubble in the bottom-right (the original `.genie-fab` launcher),
      so the open panel doesn't block the view of whatever the member is
      trying to look at.

## Dashboard — "Setup" page: business health gauges
- [ ] Add a new "Metrics" button on the dashboard's Setup page.
- [ ] Clicking it opens a set of dynamic tile gauges showing business health:
      - Revenue
      - Expenses
      - Others to define (net profit/margin, cash flow, outstanding
        invoices, member's affiliate/commission earnings, etc. — decide
        the full set when we scope this out)
- [ ] Data source: members will connect their bank/financial accounts via
      QuickBooks or another method (Plaid, etc. — TBD) so these gauges can
      show real, live financial + business metrics at a glance rather than
      static placeholders.
